# Copie para .env e preencha (.env está no .gitignore)
#
# IP: na VM, rode `ip a` e use o enp0s8 (Host-only, ex. 192.168.56.x)
# Senha: a mesma definida em POSTGRES_PASSWORD ao criar o container jornal-db

DATABASE_URL=postgresql://jornal:SUA_SENHA@192.168.56.102:5432/jornal_insights
