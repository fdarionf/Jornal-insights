[project]
name = "jornal-insights"
version = "0.1.0"
description = ""
authors = [
    {name = "Darion Forte",email = "85178125+fdarionf@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx (>=0.28.1,<0.29.0)",
    "feedparser (>=6.0.12,<7.0.0)"
]

[tool.poetry]
packages = [{include = "jornal_insights", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ruff (>=0.15.20,<0.16.0)"
]
